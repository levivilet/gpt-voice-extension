# gpt-voice-node-bridge

This package is retained for historical compatibility only.

The GPT Voice extension no longer starts or depends on a local token server.
Token creation is performed directly from the extension worker using OpenAI APIs.
